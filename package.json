{
	"name": "new-cors",
	"version": "0.0.1",
	"author": "Richard Davis",
	"description": "Modern update to the Express CORS package",
	"license": "MIT",
	"main": "index.ts",
	"scripts": {
		"build": "npm run clean && tsc",
		"clean": "rimraf dist",
		"start": "npm run build && node dist/server.js",
		"test": "echo \"Error: no test specified\" && exit 1",
		"watch": "npm run clean && tsc --watch"
	},
	"keywords": [
		"cors",
		"express",
		"modern",
		"updated",
		"middleware"
	],
	"devDependencies": {
		"@types/express": "^4.17.21",
		"@types/node": "^20.14.2",
		"rimraf": "^5.0.7",
		"ts-node": "^10.9.2",
		"typescript": "^5.4.5"
	},
	"_moduleAliases": {
		"@utils": "dist/utils"
	}
}
